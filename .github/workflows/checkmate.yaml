name: Checkmate

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened

jobs:
  pr-comment:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
      with:
        go-version: 1.18
    - run: go run main.go