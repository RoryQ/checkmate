name: Checkmate

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened

jobs:
  pr-comment:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - uses: roryq/checkmate@test-shim